import { describe,expect,it } from "vitest";
import { attentionFlags,canTransitionShipment,receiptLimit,receivingState } from "./shipmentRules";
describe("shipment lifecycle",()=>{it("chỉ cho phép chuyển trạng thái xác định",()=>{expect(canTransitionShipment("planned","booked")).toBe(true);expect(canTransitionShipment("planned","received")).toBe(false);expect(canTransitionShipment("completed","cancelled")).toBe(false);});});
describe("receiving",()=>{it("tính partial, short closure và over",()=>{expect(receivingState(100,60)).toBe("partially_received");expect(receivingState(100,95,true)).toBe("short_closed");expect(receivingState(100,103)).toBe("over_received");expect(receiptLimit(100,60,2)).toEqual({remaining:40,maximum:42,requiresReasonAt:40});});it("phản ánh reversal từ tổng posted",()=>{expect(receivingState(100,60)).toBe("partially_received");expect(receivingState(100,0)).toBe("not_received");});});
describe("attention",()=>{it("gắn cờ ETA, chứng từ, customs và hàng chưa nhận",()=>{const flags=attentionFlags({originalEta:"2026-09-10",currentEta:"2026-09-14",actualArrival:"2026-09-10",updatedAt:"2026-08-01",customsStatus:"issue",missingDocuments:["C/O"],received:0,expected:100,now:new Date("2026-09-14T00:00:00Z"),etaDelayDays:1,customsAttentionDays:5,arrivalNotReceivedDays:2,staleShipmentDays:14});expect(flags.map((x)=>x.code)).toEqual(expect.arrayContaining(["eta_delayed","document_missing","customs_issue","arrival_not_received","stale"]));});});
