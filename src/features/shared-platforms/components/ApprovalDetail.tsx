"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import Link from "next/link";import { useCallback,useEffect,useState } from "react";import { Button } from "@/components/shared/Button";import { Card } from "@/components/shared/Card";import { Textarea,Input } from "@/components/shared/FormControls";import { EmptyState,ErrorState,LoadingState } from "@/components/shared/States";import { StatusBadge } from "@/components/shared/StatusBadge";import type { ApprovalCase } from "@/features/shared-platforms/types";
export function ApprovalDetail({id}:{id:string}){const[item,setItem]=useState<ApprovalCase>(),[comment,setComment]=useState(""),[target,setTarget]=useState(""),[reason,setReason]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");const load=useCallback(async()=>{const response=await fetch(`/api/v1/approvals/${id}`),body=await response.json() as{data?:ApprovalCase;error?:{message:string}};setItem(body.data);setError(response.ok?"":body.error?.message??"Không thể tải yêu cầu.");},[id]);useEffect(()=>{void load();},[load]);async function act(action:"approve"|"reject"){setBusy(true);const response=await fetch(`/api/v1/approvals/${id}/${action}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({comment:comment||undefined})}),body=await response.json() as{error?:{message:string}};setError(response.ok?"":body.error?.message??"Không thể xử lý.");if(response.ok)await load();setBusy(false);}async function reassign(){setBusy(true);const response=await fetch(`/api/v1/approvals/${id}/reassign`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({toAccountId:target,reason})}),body=await response.json() as{error?:{message:string}};setError(response.ok?"":body.error?.message??"Không thể chuyển người duyệt.");if(response.ok){setTarget("");setReason("");await load();}setBusy(false);}if(!item&&!error)return<LoadingState/>;if(!item)return<ErrorState description={error}/>;return <div className="approval-detail"><Card><div className="detail-heading"><div><span>{item.domainType}</span><h2>{item.referenceNumber}</h2></div><StatusBadge tone={item.status==="approved"?"success":item.status==="rejected"?"error":"warning"}>{item.status}</StatusBadge></div><dl className="detail-list"><div><dt>Người tạo</dt><dd>{item.requesterName}</dd></div><div><dt>Thời gian tạo</dt><dd>{new Intl.DateTimeFormat("vi-VN",{dateStyle:"medium",timeStyle:"short"}).format(new Date(item.submittedAt))}</dd></div><div><dt>Nội dung</dt><dd>{item.summary}</dd></div><div><dt>Workflow</dt><dd>Phiên bản {item.workflowVersion}</dd></div></dl><Link className="button button--secondary button--md" href={item.domainLink}>Xem chi tiết nghiệp vụ</Link></Card><Card><h2>Tiến trình phê duyệt</h2><ol className="approval-timeline">{item.steps.map(step=><li className={`is-${step.status}`} key={step.id}><span>{step.stepOrder}</span><div><strong>{step.stepName}</strong><p>{step.approverName??"Chưa xác định người duyệt"}</p>{step.actedAt?<time dateTime={step.actedAt}>{new Intl.DateTimeFormat("vi-VN",{dateStyle:"short",timeStyle:"short"}).format(new Date(step.actedAt))}</time>:null}{step.comment?<blockquote>{step.comment}</blockquote>:null}</div><StatusBadge tone={step.status==="approved"?"success":step.status==="rejected"?"error":"warning"}>{step.status}</StatusBadge></li>)}</ol></Card>{item.status==="pending"?<Card><h2>Xử lý yêu cầu</h2>{error?<p className="form-error" role="alert">{error}</p>:null}<Textarea label="Ý kiến" value={comment} onChange={event=>setComment(event.target.value)}/><div className="form-actions"><Button disabled={busy} onClick={()=>void act("reject")} variant="danger">Từ chối</Button><Button disabled={busy} onClick={()=>void act("approve")} variant="primary">Duyệt</Button></div><details className="reassign-panel"><summary>Chuyển người duyệt</summary><div className="form-grid"><Input label="ID tài khoản mới" value={target} onChange={event=>setTarget(event.target.value)}/><Input label="Lý do" value={reason} onChange={event=>setReason(event.target.value)}/></div><Button disabled={busy||!target||reason.length<3} onClick={()=>void reassign()}>Xác nhận chuyển</Button></details></Card>:<EmptyState title="Yêu cầu đã hoàn tất"/>}</div>}
