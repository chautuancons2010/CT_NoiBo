"use client";
import Link from "next/link";import { useCallback,useEffect,useState } from "react";import { Card } from "@/components/shared/Card";import { EmptyState,ErrorState,LoadingState } from "@/components/shared/States";import { StatusBadge } from "@/components/shared/StatusBadge";import type { ApprovalCase } from "@/features/shared-platforms/types";import { useDomainReconciliation } from "@/lib/realtime/useDomainReconciliation";
const labels:Record<string,string>={LEAVE:"Nghỉ phép",TIMESHEET_ADJUSTMENT:"Điều chỉnh công",WAREHOUSE:"Kho",IMPORT_EXPORT:"Xuất nhập khẩu"};
function waitLabel(hours:number){if(hours<1)return"Mới gửi";if(hours<24)return`Chờ ${hours} giờ`;return`Chờ ${Math.floor(hours/24)} ngày`;}
export function ApprovalCenter({view}:{view:"pending"|"completed"|"delegated"}){const[items,setItems]=useState<ApprovalCase[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);const response=await fetch(`/api/v1/approvals?view=${view}`),body=await response.json() as{data?:ApprovalCase[];error?:{message:string}};setItems(body.data??[]);setError(response.ok?"":body.error?.message??"Không thể tải yêu cầu.");setLoading(false);},[view]);useEffect(()=>{queueMicrotask(()=>void load());},[load]);useDomainReconciliation("approvals",load);if(loading)return<LoadingState/>;if(error)return<ErrorState description={error}/>;if(!items.length)return<EmptyState title={view==="pending"?"Không có yêu cầu đang chờ":"Chưa có yêu cầu"}/>;return <Card className="platform-table-card"><div className="platform-table platform-table--approvals" role="table" aria-label="Yêu cầu phê duyệt"><div className="platform-table__head" role="row"><span>Loại yêu cầu</span><span>Mã tham chiếu</span><span>Người gửi</span><span>Đơn vị</span><span>Nội dung</span><span>Bước hiện tại</span><span>Thời gian chờ</span></div>{items.map(item=><Link className="platform-table__row" href={`/approvals/${item.id}`} key={item.id} role="row"><span data-label="Loại yêu cầu">{labels[item.domainType]??item.domainType}</span><strong data-label="Mã tham chiếu">{item.referenceNumber}</strong><span data-label="Người gửi">{item.requesterName}</span><span data-label="Đơn vị">{item.organizationName??"—"}</span><span data-label="Nội dung">{item.summary}</span><span data-label="Bước hiện tại">{item.currentStep?`Bước ${item.currentStep}`:<StatusBadge tone={item.status==="approved"?"success":item.status==="rejected"?"error":"neutral"}>{item.status}</StatusBadge>}</span><time dateTime={item.submittedAt} data-label="Thời gian chờ">{item.status==="pending"?waitLabel(item.waitingHours):new Intl.DateTimeFormat("vi-VN").format(new Date(item.completedAt??item.submittedAt))}</time></Link>)}</div></Card>}
