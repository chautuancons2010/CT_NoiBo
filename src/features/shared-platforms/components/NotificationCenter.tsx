"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import Link from "next/link";import { useCallback,useEffect,useState } from "react";import { Bell,CheckCheck } from "lucide-react";import { Button } from "@/components/shared/Button";import { Card } from "@/components/shared/Card";import { EmptyState,ErrorState,LoadingState } from "@/components/shared/States";import { StatusBadge } from "@/components/shared/StatusBadge";import type { AppNotification } from "@/features/shared-platforms/types";
function group(date:string){const days=Math.floor((Date.now()-new Date(date).getTime())/86400000);return days<1?"Hôm nay":days<7?"Gần đây":"Trước đó";}
export function NotificationCenter({unreadOnly=false}:{unreadOnly?:boolean}){const[items,setItems]=useState<AppNotification[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);const response=await fetch(`/api/v1/notifications${unreadOnly?"?unread=true":""}`),body=await response.json() as{data?:AppNotification[];error?:{message:string}};setItems(body.data??[]);setError(response.ok?"":body.error?.message??"Không thể tải thông báo.");setLoading(false);},[unreadOnly]);useEffect(()=>{void load();const refresh=()=>void load();window.addEventListener("focus",refresh);return()=>window.removeEventListener("focus",refresh);},[load]);async function read(id:string){await fetch(`/api/v1/notifications/${id}/read`,{method:"POST"});setItems(current=>unreadOnly?current.filter(item=>item.id!==id):current.map(item=>item.id===id?{...item,readAt:new Date().toISOString()}:item));}async function readAll(){await fetch("/api/v1/notifications/read-all",{method:"POST"});await load();}if(loading)return<LoadingState/>;if(error)return<ErrorState description={error}/>;return <Card className="notification-center"><div className="panel-header"><h2>{unreadOnly?"Chưa đọc":"Tất cả thông báo"}</h2><Button leftIcon={<CheckCheck size={16}/>} onClick={()=>void readAll()}>Đánh dấu tất cả đã đọc</Button></div>{items.length?<div className="notification-groups">{[...new Set(items.map(item=>group(item.createdAt)))].map(section=><section key={section}><h3>{section}</h3><div className="notification-list">{items.filter(item=>group(item.createdAt)===section).map(item=><article className={`notification-item ${item.readAt?"":"is-unread"}`} key={item.id}><span className="notification-item__dot"/><span className="notification-item__icon"><Bell size={18}/></span><div className="notification-item__body"><h3>{item.title}</h3><p>{item.message}</p><time dateTime={item.createdAt}>{new Intl.DateTimeFormat("vi-VN",{timeStyle:"short",dateStyle:"short"}).format(new Date(item.createdAt))}</time></div><StatusBadge tone={item.priority==="critical"?"error":item.priority==="important"?"warning":"neutral"}>{item.priority}</StatusBadge><div className="notification-item__actions">{!item.readAt?<Button size="sm" onClick={()=>void read(item.id)}>Đã đọc</Button>:null}{item.deepLink?<Link className="button button--ghost button--sm" href={item.deepLink} onClick={()=>void read(item.id)}>Mở</Link>:null}</div></article>)}</div></section>)}</div>:<EmptyState title="Chưa có thông báo"/>}</Card>}
