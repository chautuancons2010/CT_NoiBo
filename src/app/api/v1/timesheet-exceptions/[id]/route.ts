import { exceptionResolutionSchema } from "@/features/timesheets/schemas/timesheetSchemas";import { resolveException } from "@/features/timesheets/services/timesheetRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{return successResponse(await resolveException(requireAuthenticatedUser(await getRequestUser()),(await params).id,await parseJsonBody(request,exceptionResolutionSchema)));}catch(error){return errorResponse(error);}}
