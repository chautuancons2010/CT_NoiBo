import { z } from "zod"; import { uomSchema } from "@/features/warehouse/schemas/warehouseSchemas"; import { updateUom } from "@/features/warehouse/services/warehouseRepository"; import { errorResponse } from "@/lib/api/errors"; import { successResponse } from "@/lib/api/responses"; import { parseJsonBody,parseWithSchema } from "@/lib/api/validation"; import { getRequestUser } from "@/services/auth/getRequestUser"; import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function PATCH(request:Request,context:RouteContext<"/api/v1/uom/[id]">){try{const{id}=parseWithSchema(z.object({id:z.string().uuid()}),await context.params);return successResponse(await updateUom(requireAuthenticatedUser(await getRequestUser()),id,await parseJsonBody(request,uomSchema)));}catch(error){return errorResponse(error);}}
