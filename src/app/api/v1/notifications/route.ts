import { listNotifications } from "@/features/shared-platforms/services/notificationRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function GET(request:Request){try{const params=new URL(request.url).searchParams;return successResponse(await listNotifications(requireAuthenticatedUser(await getRequestUser()),{unreadOnly:params.get("unread")==="true"}));}catch(error){return errorResponse(error);}}
