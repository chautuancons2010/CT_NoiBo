import { uuidSchema } from "@/features/shared-platforms/schemas";import { markNotificationRead } from "@/features/shared-platforms/services/notificationRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseWithSchema } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function POST(_request:Request,context:RouteContext<"/api/v1/notifications/[id]/read">){try{return successResponse(await markNotificationRead(requireAuthenticatedUser(await getRequestUser()),parseWithSchema(uuidSchema,(await context.params).id)));}catch(error){return errorResponse(error);}}
