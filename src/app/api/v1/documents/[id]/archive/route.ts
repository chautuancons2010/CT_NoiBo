import { uuidSchema } from "@/features/shared-platforms/schemas";import { archiveDocument } from "@/features/shared-platforms/services/documentRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseWithSchema } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function POST(_request:Request,context:RouteContext<"/api/v1/documents/[id]/archive">){try{return successResponse(await archiveDocument(requireAuthenticatedUser(await getRequestUser()),parseWithSchema(uuidSchema,(await context.params).id)));}catch(error){return errorResponse(error);}}
