import { uuidSchema } from "@/features/shared-platforms/schemas";import { getDocument,getDocumentVersions } from "@/features/shared-platforms/services/documentRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseWithSchema } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function GET(_request:Request,context:RouteContext<"/api/v1/documents/[id]">){try{const id=parseWithSchema(uuidSchema,(await context.params).id),user=requireAuthenticatedUser(await getRequestUser());const[document,versions]=await Promise.all([getDocument(user,id),getDocumentVersions(user,id)]);return successResponse({document,versions});}catch(error){return errorResponse(error);}}
