import {runDailyMaintenance} from "@/features/operations/services/maintenanceService";import {getServerEnv} from "@/lib/env";import {errorResponse} from "@/lib/api/errors";
export async function POST(request:Request){const expected=getServerEnv().OPERATIONS_CRON_SECRET,provided=request.headers.get("authorization");if(!expected||expected.length<32||provided!==`Bearer ${expected}`)return Response.json({ok:false,error:{code:"AUTHENTICATION_REQUIRED",message:"Cron secret không hợp lệ.",requestId:crypto.randomUUID()}},{status:401,headers:{"cache-control":"no-store"}});try{return Response.json({ok:true,data:await runDailyMaintenance()},{headers:{"cache-control":"no-store"}});}catch(error){return errorResponse(error);}}
