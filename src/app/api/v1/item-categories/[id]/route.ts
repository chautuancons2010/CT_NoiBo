import { z } from "zod"; import { categorySchema } from "@/features/warehouse/schemas/warehouseSchemas"; import { updateCategory } from "@/features/warehouse/services/warehouseRepository"; import { errorResponse } from "@/lib/api/errors"; import { successResponse } from "@/lib/api/responses"; import { parseJsonBody,parseWithSchema } from "@/lib/api/validation"; import { getRequestUser } from "@/services/auth/getRequestUser"; import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function PATCH(request:Request,context:RouteContext<"/api/v1/item-categories/[id]">){try{const{id}=parseWithSchema(z.object({id:z.string().uuid()}),await context.params);return successResponse(await updateCategory(requireAuthenticatedUser(await getRequestUser()),id,await parseJsonBody(request,categorySchema)));}catch(error){return errorResponse(error);}}
