import { listQuerySchema,shipmentSchema } from "@/features/import-export/schemas/importExportSchemas";import { createShipment,listShipments } from "@/features/import-export/services/importExportRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody,parseWithSchema } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function GET(request:Request){try{return successResponse(await listShipments(requireAuthenticatedUser(await getRequestUser()),parseWithSchema(listQuerySchema,Object.fromEntries(new URL(request.url).searchParams))));}catch(error){return errorResponse(error);}}
export async function POST(request:Request){try{return successResponse(await createShipment(requireAuthenticatedUser(await getRequestUser()),await parseJsonBody(request,shipmentSchema)),{status:201});}catch(error){return errorResponse(error);}}
