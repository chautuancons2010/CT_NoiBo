import { delegationSchema } from "@/features/shared-platforms/schemas";import { createDelegation,listDelegations } from "@/features/shared-platforms/services/configurationRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function GET(){try{return successResponse(await listDelegations(requireAuthenticatedUser(await getRequestUser())));}catch(error){return errorResponse(error);}}export async function POST(request:Request){try{return successResponse(await createDelegation(requireAuthenticatedUser(await getRequestUser()),await parseJsonBody(request,delegationSchema)),{status:201});}catch(error){return errorResponse(error);}}
