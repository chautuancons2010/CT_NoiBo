import { periodVersionSchema } from "@/features/timesheets/schemas/timesheetSchemas";import { lockPeriod } from "@/features/timesheets/services/timesheetRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const body=await parseJsonBody(request,periodVersionSchema);return successResponse(await lockPeriod(requireAuthenticatedUser(await getRequestUser()),(await params).id,body.rowVersion));}catch(error){return errorResponse(error);}}
