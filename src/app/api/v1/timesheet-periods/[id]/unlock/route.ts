import { periodUnlockSchema } from "@/features/timesheets/schemas/timesheetSchemas";import { unlockPeriod } from "@/features/timesheets/services/timesheetRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const body=await parseJsonBody(request,periodUnlockSchema);return successResponse(await unlockPeriod(requireAuthenticatedUser(await getRequestUser()),(await params).id,body.rowVersion,body.reason));}catch(error){return errorResponse(error);}}
