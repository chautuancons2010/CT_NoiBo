import { shiftAssignmentInputSchema } from "@/features/timesheets/schemas/timesheetSchemas";import { saveShiftAssignment } from "@/features/timesheets/services/timesheetRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function POST(request:Request){try{return successResponse(await saveShiftAssignment(requireAuthenticatedUser(await getRequestUser()),await parseJsonBody(request,shiftAssignmentInputSchema)),{status:201});}catch(error){return errorResponse(error);}}
