import { warehouseListQuerySchema } from "@/features/warehouse/schemas/warehouseSchemas"; import { listLedger } from "@/features/warehouse/services/warehouseRepository"; import { errorResponse } from "@/lib/api/errors"; import { successResponse } from "@/lib/api/responses"; import { parseWithSchema } from "@/lib/api/validation"; import { getRequestUser } from "@/services/auth/getRequestUser"; import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function GET(request:Request){try{return successResponse(await listLedger(requireAuthenticatedUser(await getRequestUser()),parseWithSchema(warehouseListQuerySchema,Object.fromEntries(new URL(request.url).searchParams))));}catch(error){return errorResponse(error);}}
