import { approvalDecisionSchema,uuidSchema } from "@/features/shared-platforms/schemas";import { decideApprovalCase } from "@/features/shared-platforms/services/approvalRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody,parseWithSchema } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function POST(request:Request,context:RouteContext<"/api/v1/approvals/[id]/approve">){try{const id=parseWithSchema(uuidSchema,(await context.params).id),input=await parseJsonBody(request,approvalDecisionSchema);return successResponse(await decideApprovalCase(requireAuthenticatedUser(await getRequestUser()),id,"approve",input.comment));}catch(error){return errorResponse(error);}}
