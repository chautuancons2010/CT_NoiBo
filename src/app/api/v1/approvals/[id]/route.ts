import { getApprovalCase } from "@/features/shared-platforms/services/approvalRepository";import { uuidSchema } from "@/features/shared-platforms/schemas";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseWithSchema } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function GET(_request:Request,context:RouteContext<"/api/v1/approvals/[id]">){try{const id=parseWithSchema(uuidSchema,(await context.params).id);return successResponse(await getApprovalCase(requireAuthenticatedUser(await getRequestUser()),id));}catch(error){return errorResponse(error);}}
