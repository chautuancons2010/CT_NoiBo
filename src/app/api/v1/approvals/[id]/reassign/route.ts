import { approvalReassignSchema,uuidSchema } from "@/features/shared-platforms/schemas";import { reassignApprovalCase } from "@/features/shared-platforms/services/approvalRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody,parseWithSchema } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function POST(request:Request,context:RouteContext<"/api/v1/approvals/[id]/reassign">){try{const id=parseWithSchema(uuidSchema,(await context.params).id),input=await parseJsonBody(request,approvalReassignSchema);return successResponse(await reassignApprovalCase(requireAuthenticatedUser(await getRequestUser()),id,input.toAccountId,input.reason));}catch(error){return errorResponse(error);}}
