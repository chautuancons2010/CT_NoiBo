import { listApprovalCases } from "@/features/shared-platforms/services/approvalRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function GET(request:Request){try{const view=new URL(request.url).searchParams.get("view");return successResponse(await listApprovalCases(requireAuthenticatedUser(await getRequestUser()),view==="completed"||view==="delegated"?view:"pending"));}catch(error){return errorResponse(error);}}
