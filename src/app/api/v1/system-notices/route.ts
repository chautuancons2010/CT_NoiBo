import { systemNoticeSchema } from "@/features/shared-platforms/schemas";import { createSystemNotice,listSystemNotices } from "@/features/shared-platforms/services/configurationRepository";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function GET(){try{return successResponse(await listSystemNotices(requireAuthenticatedUser(await getRequestUser())));}catch(error){return errorResponse(error);}}export async function POST(request:Request){try{return successResponse(await createSystemNotice(requireAuthenticatedUser(await getRequestUser()),await parseJsonBody(request,systemNoticeSchema)),{status:201});}catch(error){return errorResponse(error);}}
