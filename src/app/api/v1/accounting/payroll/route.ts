import { payrollCreateSchema } from "@/features/accounting/schemas";import { createPayroll,listPayroll } from "@/features/accounting/service";import { errorResponse } from "@/lib/api/errors";import { successResponse } from "@/lib/api/responses";import { parseJsonBody } from "@/lib/api/validation";import { getRequestUser } from "@/services/auth/getRequestUser";import { requireAuthenticatedUser } from "@/services/authorization/requirePermission";
export async function GET(){try{return successResponse(await listPayroll(requireAuthenticatedUser(await getRequestUser())));}catch(error){return errorResponse(error);}}
export async function POST(request:Request){try{return successResponse(await createPayroll(requireAuthenticatedUser(await getRequestUser()),await parseJsonBody(request,payrollCreateSchema)),{status:201});}catch(error){return errorResponse(error);}}
