import { PageHeader } from "@/components/shared/PageHeader"; import { PermissionDeniedState } from "@/components/shared/States"; import { StockCountDetail } from "@/features/warehouse"; import { can } from "@/lib/auth/permissions"; import { getRequestUser } from "@/services/auth/getRequestUser";
export default async function Page({params}:{params:Promise<{id:string}>}){const user=await getRequestUser();if(!user||!can(user.permissions,"warehouse.stock_count.view"))return <PermissionDeniedState/>;return <div className="page-stack"><PageHeader title="Chi tiết kiểm kê"/><StockCountDetail canEdit={can(user.permissions,"warehouse.stock_count.create")} canPost={can(user.permissions,"warehouse.stock_count.post")} id={(await params).id}/></div>;}
