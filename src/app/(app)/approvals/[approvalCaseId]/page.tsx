import { BackLink } from "@/components/shared/BackLink";import { PageHeader } from "@/components/shared/PageHeader";import { ApprovalDetail } from "@/features/shared-platforms/components/ApprovalDetail";export default async function Page({params}:{params:Promise<{approvalCaseId:string}>}){const id=(await params).approvalCaseId;return <div className="page-stack"><BackLink href="/approvals/pending"/><PageHeader title="Chi tiết phê duyệt"/><ApprovalDetail id={id}/></div>}
