import { PageHeader } from "@/components/shared/PageHeader";import { ContractDetailView,ContractEditor } from "@/features/import-export/components/ImportExportViews";export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{edit?:string}>}){const{id}=await params,{edit}=await searchParams;return edit==="1"?<div className="page-stack"><PageHeader title="Chỉnh sửa hợp đồng mua hàng"/><ContractEditor id={id}/></div>:<ContractDetailView id={id}/>;}
