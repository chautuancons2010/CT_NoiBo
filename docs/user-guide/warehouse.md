# Hướng dẫn Kho — release 0.1.0

Kiểm tra warehouse scope, item/UOM và reference trước tạo chứng từ. Draft có thể sửa; **Ghi sổ** tạo ledger và không sửa trực tiếp sau đó. Sai chứng từ đã post phải **Đảo chứng từ**/điều chỉnh có lý do. Kiểm kê: expected → counted → variance → post. XNK receipt phải theo remaining quantity.

Opening stock migration phải qua chứng từ opening và business reconciliation; tuyệt đối không update bảng balance.
